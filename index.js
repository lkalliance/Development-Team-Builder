init();

function init() {
    // Welcome the user
    // call to ask for manager
    // initiate directory array
}

function getManager(directory) {
    // do an Inquire for manager
    // put on directory
    // ask if want new employee
}

function getEmployee(type, directory) {
    // do an Inquire for employee type
    // do an Inquire for employee stuff
    // put on directory
    // ask if want new employee
}

function writeHTML(directory) {
    // build the HTML document from directory
}